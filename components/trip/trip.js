import { View } from "native-base"
import React, {Component} from "react"
import {Text} from "native-base"

const Trip = () => {
    return (
      <View>
        <Text>Trip</Text>
      </View>
    );
  }

export default Trip